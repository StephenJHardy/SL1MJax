# Frozen v2 publication entry points

Snapshot of the current SPW-4 notebook and rendered summary, taken before
any survey v3 notebook/render switch. The compact v2 data bundle remains
at `src/sl1mjax/data/vla_c_band_beam_validation_v2` and must not be
overwritten (checksum `fa01cc15…`).

These copies are historical. The live notebook and
`docs/vla_c_band_beam_validation.md` now render the survey v3 pack.
The compact v2 bundle checksum remains `fa01cc15…`.
